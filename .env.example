# Copy to .env and fill in for a Postgres/Supabase-backed deployment.
# Leave unset (or delete .env) to run against the local JSONL file store --
# that's still the default and needs no setup. See README.md "Database".
DATABASE_URL=
